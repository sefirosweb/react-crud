import { Meta, StoryObj } from '@storybook/react';
import { FormTypeHtml } from '../../src/components/forms/FormTypes/FormTypeHtml';

const meta: Meta = {
  title: 'Form/FormTypes/Html',
  component: FormTypeHtml,
};

export default meta;

type Story = StoryObj<typeof FormTypeHtml>;

export const Template: Story = {}

